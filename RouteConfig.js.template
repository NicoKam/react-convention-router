import React from "react";
import { history, asyncComponent, RouterWithChildren } from "react-convention-router/tools";

const routeConfig = @routeConfig;

export default () => (
  <RouterWithChildren history={history} routes={routeConfig} />
)
